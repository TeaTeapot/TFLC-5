# *Лабораторная работа 2. Разработка лексического анализатора (сканера)*


*Выполнил студент гр. АВТ-314 Гладышев Р.М.*

## *Цель работы.*
Изучить назначение и принципы работы лексического анализатора в структуре компилятора. Спроектировать алгоритм (диаграмму состояний) и выполнить программную реализацию сканера для выделения лексем из входного текста. Интегрировать разработанный модуль в ранее созданный графический интерфейс языкового процессора.


## *Постановка задачи.*
Разработать лексический анализатор (сканер) в соответствии с индивидуальным вариантом задания, интегрировать его в приложение из лабораторной работы №1 и обеспечить наглядный вывод результатов.

*Требования к реализации сканера:*

1. Спроектировать диаграмму состояний конечного автомата, реализующего сканер, согласно варианту задания.

2. Разработать программный модуль лексического анализа, который:

  2.1. принимает на вход строку (исходный текст программы);

  2.2 выделяет все допустимые лексемы согласно варианту;

  2.3 классифицирует лексемы по типам (например: "ключевое слово", "идентификатор", "число", "оператор", "разделитель").

  2.4 любые символы, не соответствующие ни одному из допустимых типов лексем, считать недопустимыми и выводить сообщение об ошибке с указанием позиции.

  2.5 учитывает многострочность входного текста.

3. Требования к интеграции и интерфейсу:

  3.1. Встроить сканер в ранее разработанный интерфейс и связать его с кнопкой "Пуск" и соответствующим пунктом меню.

  3.2 Область вывода результатов (элемент DataGridView или аналогичная таблица) должна содержать следующие столбцы:

  3.3 Условный код (числовой идентификатор типа лексемы).

  3.4 Тип лексемы (текстовое описание).

  3.5 Лексема (выделенная подстрока).

  3.6 Местоположение (номер строки, начальная и конечная позиция символов).

4. Реализовать навигацию по ошибкам: при щелчке на сообщении об ошибке в таблице результатов курсор в области редактирования должен устанавливаться на позицию недопустимого символа.

Входные и выходные данные:

  4.1 Вход: строка (текст программного кода), введенная пользователем в область редактирования.

  4.2 Выход: таблица, содержащая последовательность условных кодов и описаний лексем.


## *ВАРИАНТ:* Цикл while на языке F#. 

Пример сканируемого текста:

![](Screenshot/example.png)


Перечень доступных лексем:

while, do, for, if, then, else, let, in, match, function, fun, rec, module, open, type, ID (ижентификатор, NUM, FLOAT, <, >, <=, >=, =, <>, +, -, *, /, %, &&, ||, not, ::, ->, <-, ;, :, (), [], {}, |.


Устройство сканера:

![](Screenshot/scheme.png)


## Тестовые примеры

![](Screenshot/test1.png)

![](Screenshot/test2.png)

![](Screenshot/test2.2.png)
